import 'package:flutter/material.dart';

class HomeDecision extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('홈 결정')),
      body: Container(),
    );
  }
}